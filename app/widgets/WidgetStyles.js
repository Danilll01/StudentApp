import { StyleSheet, Platform } from 'react-native';

export default StyleSheet.create({
  basicWidget: {
    backgroundColor: '#EDEDED',
    marginBottom: 20,
    paddingBottom: 20,
    height: 'auto',
    borderRadius: 16,
  },
  basicWidgetHeader: {
    textAlign: 'center',
    paddingTop: 20,
    fontSize: 22,
  }
});