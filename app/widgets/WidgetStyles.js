import { StyleSheet, Platform } from 'react-native';

export default StyleSheet.create({
  basicWidget: {
    backgroundColor: 'red',
    marginBottom: 20,
    height: 300,
    borderRadius: 16,
  },
});